from Calculator.Square import square
from Calculator.Statistics.Mean import mean

def standard_deviation(data):
    num_values = len(data)
    total - 0
    for num in data
        total = square(sum(num - ))
    return mean(data)square_root(/num_values)